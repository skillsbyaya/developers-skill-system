# Project Convention Overrides

This file contains only durable project-specific differences from the global document and identifier conventions. Unmentioned rules inherit globally. Remove headings and tables that have no entries.

## Document type overrides

Each row completely replaces the global folder and naming pattern for that document type.

| doc-type | folder | naming-pattern | reason |
| --- | --- | --- | --- |
| `{type}` | `{folder}` | `{pattern}` | `{why this project differs}` |

## Document rule overrides

Use the exact global `# ...-note` key. Only the named rule changes.

| global note key | project rule | reason |
| --- | --- | --- |
| `{# key-note}` | `{replacement rule}` | `{why this project differs}` |

## Identifier convention overrides

Name the global heading and exact rule or prefix being replaced. Only that target changes.

| global section | rule or prefix | project convention | reason |
| --- | --- | --- | --- |
| `{section}` | `{target rule}` | `{replacement rule}` | `{why this project differs}` |

## Project-only additions

Use this only when the project needs a document type or identifier rule with no global counterpart. Name one clear owner and scope.

| kind | name | convention | owner and reason |
| --- | --- | --- | --- |
| `{document or identifier}` | `{name}` | `{rule}` | `{owner; why it is project-only}` |
